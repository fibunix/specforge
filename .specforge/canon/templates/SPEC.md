# Spec: <slug>

> If a DESIGN.md exists: `Builds the "<section>" from DESIGN.md`.
> Otherwise this Description IS the design — include the rationale here.

## Description
<what we're building and why. 1–3 paragraphs. No code.>

## Acceptance criteria
<observable, testable outcomes. Plain checkboxes — the text is the identity.
Keep to a handful; split the work if it grows past ~8.>
- [ ] ...
- [ ] ...

## Tests
<files the test-author will create/extend, each noting what it covers.>
- [ ] path/to/test — covers: ...

## Implementation
<files the implementer will create/change.>
- [ ] path/to/file
