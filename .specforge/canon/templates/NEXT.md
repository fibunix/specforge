# Backlog

One-line briefs for future work items. The coordinator/aligner pulls from here
when starting new work. Keep each bullet to *what* + *why*; delete when picked up.

- (example) Add a `--json` flag to the report command — so CI can parse output.
